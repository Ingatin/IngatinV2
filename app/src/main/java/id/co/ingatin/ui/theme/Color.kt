package id.co.ingatin.ui.theme

import androidx.compose.ui.graphics.Color

// Palet utama (light)
val Primary = Color(0xFF363062) // biru tua — tombol, card, chip, border kategori terpilih
val Secondary = Color(0xFF4D4C7D) // biru — container CardMyTask
val Tertiary = Color(0xFFF99417) // oren — aksen FAB, judul, dialog, spinner
val Background = Color(0xFFF7F8FD) // putih off-white — latar seluruh app
val Surface = Color(0xFFFFFFFF) // ButtonCategory terpilih
val SurfaceVariant = Color(0xFFEDEFF7) // container HomeTabs

// Konten di atas palet utama
val OnPrimary = Color(0xFFFFFFFF) // teks di atas tombol/card primary
val OnSecondary = Color(0xFFFFFFFF) // teks di atas CardMyTask
val OnTertiary = Color(0xFF000000) // konten FAB
val OnBackground = Color(0xFF2A2840) // teks utama di atas background
val OnSurface = Color(0xFF2A2840) // teks di atas surface
val OnSurfaceVariant = Color(0xFF6B6B80) // teks petunjuk/halus

// Utilitas
val Outline = Color(0xFFDCDDDD) // garis border halus
val Error = Color(0xFFB3261E) // teks/border error
